/// Location of the staff home. `/staff` in the web build; `/` in the waiter
/// app (`main_waiter.dart`), where the PIN login is the root. Set once by
/// `buildRouter`.
String staffHomePath = '/staff';
